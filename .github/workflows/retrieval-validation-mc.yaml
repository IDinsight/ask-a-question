name: Validate retrieval on MC
on:
  push:
    branches:
      - main
      - AAQ-81-retrieval-val

jobs:
  call_validation:
    uses: ./.github/workflows/retrieval-validation.yaml
    with:
      environment: validation-mc
